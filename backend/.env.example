PORT=3001
NODE_ENV=development

# GitHub Copilot / GitHub Models (Official SDK)
GITHUB_TOKEN=ghp_your_github_token_here
COPILOT_API_URL=https://models.github.ai/inference
COPILOT_MODEL=openai/gpt-4.1

# Security
ALLOWED_ORIGINS=http://localhost:5173,http://localhost

# Domain (for subdomain routing: {slug}.gilo.dev)
# Set this to your actual domain for production
GILO_DOMAIN=gilo.dev

# Database
DATABASE_URL=postgresql://gilo:gilo_dev_password@localhost:5432/gilo_ai

# Session Configuration
SESSION_TIMEOUT=3600000

# MCP Storage Directory
MCP_STORAGE_DIR=./data

# ---- Caddy / TLS Configuration ----
# Email for Let's Encrypt certificate registration
TLS_EMAIL=admin@gilo.dev

# GoDaddy API Key for wildcard TLS certificates
# Format: "key:secret" (e.g. "dLkj3abc:4kZNp2qR")
# Get yours at: https://developer.godaddy.com/keys
GODADDY_API_KEY=your_godaddy_api_key:your_godaddy_api_secret

# ---- Public Rate Limiting ----
# Requests per minute for public subdomain chat endpoints
PUBLIC_RATE_LIMIT_PER_MINUTE=30
# Requests per day for public subdomain chat endpoints
PUBLIC_RATE_LIMIT_PER_DAY=500

# ---- Admin Configuration ----
# Email of the app developer/admin (has store management rights)
ADMIN_EMAIL=your_admin_email@example.com

# Add your custom MCP server tokens here
# OPENAI_API_KEY=your_openai_key
